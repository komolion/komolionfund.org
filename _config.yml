collections:
  team:
  work:
    output: true

defaults:
  - scope:
      path: ""
      type: "work"
    values:
      layout: "work"
  - scope:
      path: ""
    values:
      layout: "default"

exclude: []

sass:
  load_paths:
    - _sass
    - node_modules
